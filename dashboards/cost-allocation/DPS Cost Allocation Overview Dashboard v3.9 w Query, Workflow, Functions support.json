{
  "name": "DPS Cost Allocation Overview Dashboard v3.9 w Query, Workflow, Functions support",
  "version": 21,
  "variables": [
    {
      "version": 2,
      "key": "CostAllocation",
      "type": "csv",
      "visible": true,
      "editable": true,
      "input": "costcenter,product",
      "multiple": false,
      "defaultValue": "costcenter"
    },
    {
      "version": 2,
      "key": "Filter",
      "type": "query",
      "visible": true,
      "editable": true,
      "input": "fetch dt.system.events\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| fieldsAdd value = if($CostAllocation == \"costcenter\", dt.cost.costcenter)\n| fieldsAdd value = if($CostAllocation == \"product\", dt.cost.product, else: value)\n\n| fieldsAdd c = coalesce(value, $unassigned)\n| expand c\n| fieldsAdd c = coalesce(c[key], c)\n| summarize c=collectDistinct(c)\n| expand c",
      "multiple": true,
      "defaultValue": [
        "3420b2ac-f1cf-4b24-b62d-61ba1ba8ed05*"
      ]
    },
    {
      "version": 2,
      "key": "Capability",
      "type": "query",
      "visible": true,
      "editable": true,
      "input": "data json:$ratecard_data\n|sort Category\n| join [\n   FETCH dt.system.events\n    | filter event.kind == \"BILLING_USAGE_EVENT\" \n    | fieldsRename name=event.type\n    | fieldsAdd hasCostAllocation = IF(isNull(dt.cost.costcenter) and isNull(dt.cost.product),false ,else: true)\n    | summarize cnt_ca=countIf(hasCostAllocation), by:name\n], on: { name }, kind: outer, prefix: \"included.\"\n| fieldsAdd rateCardFound = if(isnull(name),\"no\",else:\"yes\")\n| fieldsAdd supported = if(isNull(included.name),\"no\",else: if(included.cnt_ca==0,\"no cost allocation\", else: \"yes\"))\n| filter $SupportedCapabilitiesOnly == \"no\" OR supported == \"yes\"\n| fieldsKeep name\n\n",
      "multiple": true,
      "defaultValue": [
        "3420b2ac-f1cf-4b24-b62d-61ba1ba8ed05*"
      ]
    },
    {
      "version": 2,
      "key": "unassigned",
      "type": "text",
      "visible": false,
      "editable": true,
      "defaultValue": "unassigned"
    },
    {
      "version": 2,
      "key": "SupportedCapabilitiesOnly",
      "type": "csv",
      "visible": true,
      "editable": true,
      "input": "yes,no",
      "multiple": false,
      "defaultValue": "no"
    },
    {
      "key": "ratecard_data",
      "visible": false,
      "type": "text",
      "version": 2,
      "editable": true,
      "defaultValue": "[{\"category\":\"Automation\",\"key\":\"AUTOMATIONS\",\"name\":\"Automation Workflow\",\"unitName\":\"workflow-hours\",\"costAllocation\":\"with lookup*\"},{\"category\":\"Events powered by Grail\",\"key\":\"EVENTS_ANALYZE\",\"name\":\"Events - Query\",\"unitName\":\"gibibytes-scanned\",\"costAllocation\":\"with lookup*\"},{\"category\":\"Events powered by Grail\",\"key\":\"EVENTS_INGEST\",\"name\":\"Events - Ingest & Process\",\"unitName\":\"gibibytes\"},{\"category\":\"Events powered by Grail\",\"key\":\"EVENTS_RETAIN\",\"name\":\"Events - Retain\",\"unitName\":\"gibibyte-days\"},{\"category\":\"Metrics powered by Grail\",\"key\":\"METRICS_QUERY\",\"name\":\"Metrics - Query\",\"unitName\":\"n/a\",\"costAllocation\":\"with lookup*\",\"BUE\":\"no\"},{\"category\":\"Metrics powered by Grail\",\"key\":\"METRICS_INGEST\",\"name\":\"Metrics - Ingest & Process\",\"unitName\":\"metrics data points\",\"costAllocation\":\"yes\"},{\"category\":\"Metrics powered by Grail\",\"key\":\"METRICS_EXTENDED_RETAIN\",\"name\":\"Metrics - Retain\",\"unitName\":\"gibibyte-days\"},{\"category\":\"Container Observability\",\"key\":\"CODE_MONITORING\",\"name\":\"Code Monitoring\",\"unitName\":\"Container-hour\",\"costAllocation\":\"yes*\"},{\"category\":\"AppEngine Functions\",\"key\":\"COMPUTE\",\"name\":\"AppEngine Functions - Small\",\"unitName\":\"invocations\",\"costAllocation\":\"with lookup*\"},{\"category\":\"Application and Infrastructure Observability\",\"key\":\"FOUNDATION_AND_DISCOVERY\",\"name\":\"Foundation & Discovery\",\"unitName\":\"host-hours\",\"costAllocation\":\"yes\"},{\"category\":\"Application and Infrastructure Observability\",\"key\":\"FULLSTACK_MONITORING\",\"name\":\"Full-Stack Monitoring\",\"unitName\":\"memory-gibibyte-hours\",\"costAllocation\":\"yes\"},{\"category\":\"Application and Infrastructure Observability\",\"key\":\"INFRASTRUCTURE_MONITORING\",\"name\":\"Infrastructure Monitoring\",\"unitName\":\"host-hours\",\"costAllocation\":\"yes\"},{\"category\":\"Container Observability\",\"key\":\"KUBERNETES_OPERATIONS\",\"name\":\"Kubernetes Platform Monitoring\",\"unitName\":\"pod-hours\"},{\"category\":\"Log Analytics\",\"key\":\"LOG_MANAGEMENT_ANALYZE\",\"name\":\"Log Management & Analytics - Query\",\"unitName\":\"gibibytes-scanned\",\"costAllocation\":\"with lookup*\"},{\"category\":\"Log Analytics\",\"key\":\"LOG_MANAGEMENT_INGEST\",\"name\":\"Log Management & Analytics - Ingest & Process\",\"unitName\":\"gibibytes\",\"costAllocation\":\"yes\"},{\"category\":\"Log Analytics\",\"key\":\"LOG_MANAGEMENT_RETAIN\",\"name\":\"Log Management & Analytics - Retain\",\"unitName\":\"gibibyte-days\",\"costAllocation\":\"yes\"},{\"category\":\"Log Analytics\",\"key\":\"LOG_MANAGEMENT_RETAIN_QUERY_INCLUDED\",\"name\":\"Log Management & Analytics - Retain with Included Queries\",\"unitName\":\"gibibyte-days\",\"costAllocation\":\"yes\"},{\"category\":\"Application and Infrastructure Observability\",\"key\":\"MAINFRAME_MONITORING\",\"name\":\"Mainframe Monitoring\",\"unitName\":\"MSU-hours\"},{\"category\":\"Application Security\",\"key\":\"RUNTIME_APPLICATION_PROTECTION\",\"name\":\"Runtime Application Protection\",\"unitName\":\"memory-gibibyte-hours\",\"costAllocation\":\"yes\"},{\"category\":\"Application Security\",\"key\":\"RUNTIME_VULNERABILITY_ANALYTICS\",\"name\":\"Runtime Vulnerability Analytics\",\"unitName\":\"memory-gibibyte-hours\",\"costAllocation\":\"yes\"},{\"category\":\"Application Security\",\"key\":\"SECURITY_POSTURE_MANAGEMENT\",\"name\":\"Security Posture Management\",\"unitName\":\"memory-gibibyte-hours\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"SYNTHETIC_MONITORING_BROWSER\",\"name\":\"Browser Monitor or Clickpath\",\"unitName\":\"synthetic actions\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"SYNTHETIC_MONITORING_HTTP\",\"name\":\"HTTP Monitor\",\"unitName\":\"synthetic requests\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"SYNTHETIC_MONITORING_THIRD_PARTY\",\"name\":\"Third-Party Synthetic API Ingestion\",\"unitName\":\"third-party synthetic results\"},{\"category\":\"Traces powered by Grail\",\"key\":\"TRACE_INGEST\",\"name\":\"Traces - Ingest & Process\",\"unitName\":\"gibibytes\",\"costAllocation\":\"yes\"},{\"category\":\"Traces powered by Grail\",\"key\":\"TRACE_QUERY\",\"name\":\"Traces - Query\",\"unitName\":\"gibibytes-scanned\",\"costAllocation\":\"with lookup*\"},{\"category\":\"Traces powered by Grail\",\"key\":\"TRACE_RETAIN\",\"name\":\"Traces - Retain\",\"unitName\":\"gibibyte-days\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"USER_SESSION_PROPERTIES\",\"name\":\"Real User Monitoring Property\",\"unitName\":\"properties per session\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"USER_SESSION_REPLAYS\",\"name\":\"Real User Monitoring with Session Replay\",\"unitName\":\"session replay captures\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"USER_SESSIONS\",\"name\":\"Real User Monitoring\",\"unitName\":\"sessions\"},{\"category\":\"Platform\",\"key\":\"DATA_EGRESS\",\"name\":\"Data Egress\",\"unitName\":\"gibibytes\"},{\"category\":\"Application and Infrastructure Observability\",\"key\":\"DATABASE_MONITORING\",\"name\":\"Database Monitoring\",\"unitName\":\"database-instance-hours\"},{\"category\":\"AppEngine Functions\",\"key\":\"AI_FUNCTIONS\",\"name\":\"AI Units\",\"unitName\":\"AI units\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"DEM_RETAIN\",\"name\":\"Digital Experience Monitoring - Retain\",\"unitName\":\"gibibyte-days\",\"costAllocation\":\"with lookup*\"},{\"category\":\"Digital Experience Monitoring\",\"key\":\"DEM_QUERY\",\"name\":\"Digital Experience Monitoring - Query\",\"unitName\":\"gibibytes-scanned\",\"costAllocation\":\"with lookup*\"},{\"category\":\"Events powered by Grail\",\"key\":\"EVENTS_RETAIN\",\"name\":\"Files - Retain\",\"unitName\":\"gibibyte-days\"}]"
    },
    {
      "version": 2,
      "key": "resolution",
      "type": "csv",
      "visible": true,
      "editable": true,
      "input": "15m,1h,1d,1w",
      "multiple": false,
      "defaultValue": [
        "3420b2ac-f1cf-4b24-b62d-61ba1ba8ed05*"
      ]
    },
    {
      "version": 2,
      "key": "price_points",
      "type": "text",
      "visible": false,
      "editable": true,
      "defaultValue": "[{\"key\": \"FOUNDATION_AND_DISCOVERY\", \"name\": \"Foundation & Discovery\", \"price\": \"1000.00\", \"Category\": \"Application and Infrastructure Observability\", \"unitName\": \"host-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"FULLSTACK_MONITORING\", \"name\": \"Full-Stack Monitoring\", \"price\": \"1000.00\", \"Category\": \"Application and Infrastructure Observability\", \"unitName\": \"memory-gibibyte-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"INFRASTRUCTURE_MONITORING\", \"name\": \"Infrastructure Monitoring\", \"price\": \"4000.00\", \"Category\": \"Application and Infrastructure Observability\", \"unitName\": \"host-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"MAINFRAME_MONITORING\", \"name\": \"Mainframe Monitoring\", \"price\": \"1000.00\", \"Category\": \"Application and Infrastructure Observability\", \"unitName\": \"MSU-hours\", \"priceUnit\": \"10000\", \"currencyCode\": \"USD\"}, {\"key\": \"CODE_MONITORING\", \"name\": \"Code Monitoring\", \"price\": \"500.00\", \"Category\": \"Container Observability\", \"unitName\": \"Container-hour\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"KUBERNETES_OPERATIONS\", \"name\": \"Kubernetes Platform Monitoring\", \"price\": \"200.00\", \"Category\": \"Container Observability\", \"unitName\": \"pod-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"LOG_MANAGEMENT_INGEST\", \"name\": \"Log Management & Analytics - Ingest & Process\", \"price\": \"2000.00\", \"Category\": \"Log Analytics\", \"unitName\": \"gibibytes\", \"priceUnit\": \"10000\", \"currencyCode\": \"USD\"}, {\"key\": \"LOG_MANAGEMENT_ANALYZE\", \"name\": \"Log Management & Analytics - Query\", \"price\": \"3500.00\", \"Category\": \"Log Analytics\", \"unitName\": \"gibibytes-scanned\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"LOG_MANAGEMENT_RETAIN\", \"name\": \"Log Management & Analytics - Retain\", \"price\": \"700.00\", \"Category\": \"Log Analytics\", \"unitName\": \"gibibyte-days\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"LOG_MANAGEMENT_RETAIN_QUERY_INCLUDED\", \"name\": \"Log Management & Analytics - Retain with Included Queries\", \"price\": \"20000.00\", \"Category\": \"Log Analytics\", \"unitName\": \"gibibyte-days\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"RUNTIME_APPLICATION_PROTECTION\", \"name\": \"Runtime Application Protection\", \"price\": \"225.00\", \"Category\": \"Application Security\", \"unitName\": \"memory-gibibyte-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"RUNTIME_VULNERABILITY_ANALYTICS\", \"name\": \"Runtime Vulnerability Analytics\", \"price\": \"225.00\", \"Category\": \"Application Security\", \"unitName\": \"memory-gibibyte-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"SECURITY_POSTURE_MANAGEMENT\", \"name\": \"Security Posture Management\", \"price\": \"700.00\", \"Category\": \"Application Security\", \"unitName\": \"host-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"SYNTHETIC_MONITORING_BROWSER\", \"name\": \"Browser Monitor or Clickpath\", \"price\": \"450.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"synthetic actions\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"SYNTHETIC_MONITORING_HTTP\", \"name\": \"HTTP Monitor\", \"price\": \"100.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"synthetic requests\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"USER_SESSIONS\", \"name\": \"Real User Monitoring\", \"price\": \"225.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"sessions\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"USER_SESSION_PROPERTIES\", \"name\": \"Real User Monitoring Property\", \"price\": \"10.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"properties per session\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"USER_SESSION_REPLAYS\", \"name\": \"Real User Monitoring with Session Replay\", \"price\": \"450.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"session replay captures\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"SYNTHETIC_MONITORING_THIRD_PARTY\", \"name\": \"Third-Party Synthetic API Ingestion\", \"price\": \"100.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"third-party synthetic results\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"METRICS_INGEST\", \"name\": \"Metrics - Ingest & Process\", \"price\": \"150.00\", \"Category\": \"Metrics powered by Grail\", \"unitName\": \"metric data points\", \"priceUnit\": \"100000000\", \"currencyCode\": \"USD\"}, {\"key\": \"METRICS_QUERY\", \"name\": \"Metrics - Query\", \"price\": \"0.00\", \"Category\": \"Metrics powered by Grail\", \"unitName\": \"gibibytes-scanned\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"METRICS_EXTENDED_RETAIN\", \"name\": \"Metrics - Retain\", \"price\": \"700.00\", \"Category\": \"Metrics powered by Grail\", \"unitName\": \"gibibyte-days\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"TRACE_INGEST\", \"name\": \"Traces - Ingest & Process\", \"price\": \"2000.00\", \"Category\": \"Traces powered by Grail\", \"unitName\": \"gibibytes\", \"priceUnit\": \"10000\", \"currencyCode\": \"USD\"}, {\"key\": \"TRACE_QUERY\", \"name\": \"Traces - Query\", \"price\": \"3500.00\", \"Category\": \"Traces powered by Grail\", \"unitName\": \"gibibytes-scanned\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"TRACE_RETAIN\", \"name\": \"Traces - Retain\", \"price\": \"700.00\", \"Category\": \"Traces powered by Grail\", \"unitName\": \"gibibyte-days\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"EVENTS_INGEST\", \"name\": \"Events - Ingest & Process\", \"price\": \"2000.00\", \"Category\": \"Events powered by Grail\", \"unitName\": \"gibibytes\", \"priceUnit\": \"10000\", \"currencyCode\": \"USD\"}, {\"key\": \"EVENTS_ANALYZE\", \"name\": \"Events - Query\", \"price\": \"3500.00\", \"Category\": \"Events powered by Grail\", \"unitName\": \"gibibytes-scanned\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"EVENTS_RETAIN\", \"name\": \"Events - Retain\", \"price\": \"700.00\", \"Category\": \"Events powered by Grail\", \"unitName\": \"gibibyte-days\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"EVENTS_RETAIN\", \"name\": \"Files - Retain\", \"price\": \"700.00\", \"Category\": \"Events powered by Grail\", \"unitName\": \"gibibyte-days\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"AUTOMATIONS\", \"name\": \"Automation Workflow\", \"price\": \"300.00\", \"Category\": \"Automation\", \"unitName\": \"workflow-hours\", \"priceUnit\": \"10000\", \"currencyCode\": \"USD\"}, {\"key\": \"COMPUTE\", \"name\": \"AppEngine Functions - Small\", \"price\": \"1000.00\", \"Category\": \"AppEngine Functions\", \"unitName\": \"invocations\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"CUSTOM_EVENTS_CLASSIC\", \"name\": \"Custom Events Classic\", \"price\": \"200.00\", \"Category\": \"Platform Extensions\", \"unitName\": \"custom events\", \"priceUnit\": \"100000000\", \"currencyCode\": \"USD\"}, {\"key\": \"CUSTOM_METRICS_CLASSIC\", \"name\": \"Custom Metrics Classic\", \"price\": \"200.00\", \"Category\": \"Platform Extensions\", \"unitName\": \"metric data points\", \"priceUnit\": \"100000000\", \"currencyCode\": \"USD\"}, {\"key\": \"CUSTOM_TRACES_CLASSIC\", \"name\": \"Custom Traces Classic\", \"price\": \"140.00\", \"Category\": \"Platform Extensions\", \"unitName\": \"spans\", \"priceUnit\": \"100000000\", \"currencyCode\": \"USD\"}, {\"key\": \"LOG_MONITORING_CLASSIC\", \"name\": \"Log Monitoring Classic\", \"price\": \"100.00\", \"Category\": \"Platform Extensions\", \"unitName\": \"log records\", \"priceUnit\": \"100000000\", \"currencyCode\": \"USD\"}, {\"key\": \"SERVERLESS_FUNCTIONS_CLASSIC\", \"name\": \"Serverless Functions Classic\", \"price\": \"400.00\", \"Category\": \"Platform Extensions\", \"unitName\": \"invocations\", \"priceUnit\": \"100000000\", \"currencyCode\": \"USD\"}, {\"key\": \"DATA_EGRESS\", \"name\": \"Data Egress\", \"price\": \"1000.00\", \"Category\": \"Platform\", \"unitName\": \"gibibytes\", \"priceUnit\": \"10000\", \"currencyCode\": \"USD\"}, {\"key\": \"DATABASE_MONITORING\", \"name\": \"Database Monitoring\", \"price\": \"4000.00\", \"Category\": \"Application and Infrastructure Observability\", \"unitName\": \"database-instance-hours\", \"priceUnit\": \"100000\", \"currencyCode\": \"USD\"}, {\"key\": \"AI_FUNCTIONS\", \"name\": \"AI Units\", \"price\": \"1000.00\", \"Category\": \"AppEngine Functions\", \"unitName\": \"AI units\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"DEM_RETAIN\", \"name\": \"Digital Experience Monitoring - Retain\", \"price\": \"4000.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"gibibyte-days\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}, {\"key\": \"DEM_QUERY\", \"name\": \"Digital Experience Monitoring - Query\", \"price\": \"3500.00\", \"Category\": \"Digital Experience Monitoring\", \"unitName\": \"gibibytes-scanned\", \"priceUnit\": \"1000000\", \"currencyCode\": \"USD\"}]"
    }
  ],
  "tiles": {
    "0": {
      "title": "Used Rate Card and Cost Allocation Status (can be changed by editing the price_point variable <---)",
      "type": "data",
      "query": "data json:$ratecard_data\n| join [ data json:$price_points], on: key, prefix:\"price.\"\n|sort category\n| join [\n   FETCH dt.system.events\n    | filter event.kind == \"BILLING_USAGE_EVENT\" \n    | fieldsRename name=event.type\n    | fieldsAdd hasCostAllocation = IF(isNull(dt.cost.costcenter) and isNull(dt.cost.product),false ,else: true)\n    | summarize cnt_ca=countIf(hasCostAllocation), by:name\n], on: { name }, kind: outer, prefix: \"included.\"\n| fieldsAdd rateCardFound = if(isnull(name),\"no\",else:\"yes\"), ccy=price.currencyCode, price=price.price, unit=price.priceUnit\n| fieldsRemove \"price.*\"\n| fieldsAdd costAllocation = if(isNull(costAllocation),\"no\",else: costAllocation)\n| fieldsAdd usageFound = if(isNotNull(included.cnt_ca),\"yes\",else:\"no\")\n| fieldsAdd costAllocationCorrectlyConfigured = if(costAllocation==\"no\",\"n/a\", else: if(included.cnt_ca > 0,\"yes\",else:\"no\"))\n| fieldsRemove \"included.*\", key\n| sort category, name",
      "visualization": "table",
      "visualizationSettings": {
        "table": {
          "columnOrder": [
            "[\"category\"]",
            "[\"name\"]",
            "[\"unitName\"]",
            "[\"costAllocation\"]",
            "[\"BUE\"]",
            "[\"rateCardFound\"]",
            "[\"ccy\"]",
            "[\"price\"]",
            "[\"unit\"]",
            "[\"usageFound\"]",
            "[\"costAllocationCorrectlyConfigured\"]"
          ],
          "sortBy": [
            {
              "columnId": "[\"Category\"]",
              "direction": "ascending"
            }
          ]
        },
        "autoSelectVisualization": true
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "1": {
      "type": "markdown",
      "content": "## Disclaimer\n* Ensure to adjust the `price_points` variable\n* Usage of the Dashboard is free of charge\n* This Dashboard needs to be set up via the cost allocation configuration notebook. There will be errors if it's not set up"
    },
    "2": {
      "type": "markdown",
      "content": "## Change Log\n* v 3.9: Fix references to Events keys\n* v 3.8: Fix KPM usage calculation\n* v 3.7: Fix for Traces OTel ingest\n* v 3.6: Include Metrics ingest, fix filter drop down\n* v 3.5: Included RUM total usage (no Cost Allocation support – only \"unassigned\")\n* v 3.4: Fixed calculation error for Retain capabilities, included units to rate card prices for easier configuration\n* v 3.3: Adjusted for Log Retain/RwiQ & Fixed Trace Ingest billed usage\n* v 3.2: Fixed some overlooked lookup table references\n* v 3.1: Dedup events\n* v 3.0: Branched a version w/o lookup tables to ensure no related costs"
    },
    "3": {
      "title": "Estimated cost distribution per Category",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = arraySum(cost)\n| fieldsAdd usage = arraySum(usage)\n//The summarize details would change based on intended tile\n| summarize {total_usage=sum(usage), total_cost=sum(cost)}, by:{category=ratecard.Category}\n| sort total_cost desc",
      "visualization": "pieChart",
      "visualizationSettings": {
        "chartSettings": {
          "legend": {
            "position": "right"
          },
          "categoricalBarChartSettings": {
            "valueAxis": "total_cost",
            "valueAxisLabel": "total_cost",
            "categoryAxis": [
              "category"
            ],
            "categoryAxisLabel": "category"
          },
          "circleChartSettings": {
            "valueType": "relative",
            "groupingThresholdType": "number-of-slices",
            "groupingThresholdValue": 20,
            "groupingName": "Other"
          }
        },
        "legend": {
          "ratio": 52
        },
        "autoSelectVisualization": false,
        "unitsOverrides": [
          {
            "identifier": "total_usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "total_cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "4": {
      "title": "Total estimated costs of selected Capabilities and filters",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = arraySum(cost)\n| fieldsAdd usage = arraySum(usage)\n//The summarize details would change based on intended tile\n| summarize {total_usage=sum(usage), total_estimated_cost=sum(cost)}",
      "visualization": "singleValue",
      "visualizationSettings": {
        "singleValue": {
          "label": "total estimated costs",
          "recordField": "total_estimated_cost"
        },
        "autoSelectVisualization": false,
        "unitsOverrides": [
          {
            "identifier": "total_usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "total_estimated_cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "5": {
      "title": "Estimated cost distribution per Capability",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = arraySum(cost)\n| fieldsAdd usage = arraySum(usage)\n//The summarize details would change based on intended tile\n| summarize {total_usage=sum(usage), total_cost=sum(cost)}, by:{capability=event.type}\n| sort total_cost desc",
      "visualization": "pieChart",
      "visualizationSettings": {
        "chartSettings": {
          "legend": {
            "position": "right"
          },
          "categoricalBarChartSettings": {
            "valueAxis": "total_cost",
            "valueAxisLabel": "total_cost",
            "categoryAxis": [
              "capability"
            ],
            "categoryAxisLabel": "capability"
          },
          "circleChartSettings": {
            "valueType": "relative",
            "groupingThresholdType": "number-of-slices",
            "groupingThresholdValue": 20,
            "groupingName": "Other"
          }
        },
        "legend": {
          "ratio": 52
        },
        "autoSelectVisualization": false,
        "unitsOverrides": [
          {
            "identifier": "total_usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "total_cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "6": {
      "title": "List of usage and estimated costs per Capability",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = arraySum(cost)\n| fieldsAdd usage = arraySum(usage)\n//The summarize details would change based on intended tile\n| filter in(event.type, {$Capability})\n| summarize {total_usage=sum(usage), total_cost=sum(cost)}, by:{capability=event.type, category=ratecard.Category}\n| sort total_cost desc",
      "visualization": "table",
      "visualizationSettings": {
        "table": {
          "hideColumnsForLargeResults": false,
          "sortBy": [
            {
              "columnId": "[\"capability\"]",
              "direction": "ascending"
            }
          ],
          "columnWidths": {
            "[\"event.type\"]": 312,
            "[\"total_cost\"]": 88
          },
          "columnOrder": [
            "[\"category\"]",
            "[\"capability\"]",
            "[\"total_usage\"]",
            "[\"total_cost\"]"
          ]
        },
        "unitsOverrides": [
          {
            "identifier": "total_usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "total_cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "7": {
      "type": "markdown",
      "content": "## Estimated costs per $CostAllocation"
    },
    "8": {
      "title": "Estimated cost distribution per cost center",
      "description": "Showing only values above 5% of total costs",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = arraySum(cost)\n| fieldsAdd usage = arraySum(usage)\n//The summarize details would change based on intended tile\n| summarize {total_usage=sum(usage), total_cost=sum(cost)}, by:{filter}\n| sort total_cost desc",
      "visualization": "pieChart",
      "visualizationSettings": {
        "chartSettings": {
          "legend": {
            "position": "right"
          },
          "colorPalette": "fireplace-inverted",
          "categoricalBarChartSettings": {
            "valueAxis": "total_cost",
            "valueAxisLabel": "total_cost",
            "categoryAxis": [
              "capability",
              "filter"
            ],
            "categoryAxisLabel": "capability,filter"
          },
          "circleChartSettings": {
            "valueType": "relative",
            "groupingThresholdType": "number-of-slices",
            "groupingThresholdValue": 10,
            "groupingName": "Other"
          }
        },
        "legend": {
          "ratio": 52
        },
        "coloring": {
          "colorRules": [
            {
              "field": "DT.name",
              "comparator": "= *value*",
              "value": "",
              "type": "string",
              "colorMode": "color-palette",
              "colorPalette": "fireplace-inverted"
            }
          ]
        },
        "autoSelectVisualization": false,
        "unitsOverrides": [
          {
            "identifier": "total_usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "total_cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "9": {
      "title": "List of usage and estimated costs per Capability and Cost Center",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = arraySum(cost)\n| fieldsAdd usage = arraySum(usage)\n//The summarize details would change based on intended tile\n| filter in(event.type, {$Capability})\n| summarize {total_usage=sum(usage), total_cost=sum(cost)}, by:{capability=event.type,filter, category=ratecard.Category}\n| sort total_cost desc",
      "visualization": "table",
      "visualizationSettings": {
        "table": {
          "hideColumnsForLargeResults": false,
          "sortBy": [
            {
              "columnId": "[\"total_cost\"]",
              "direction": "descending"
            }
          ],
          "columnWidths": {
            "[\"event.type\"]": 312,
            "[\"total_cost\"]": 88
          },
          "columnOrder": [
            "[\"category\"]",
            "[\"capability\"]",
            "[\"filter\"]",
            "[\"total_usage\"]",
            "[\"total_cost\"]"
          ]
        },
        "unitsOverrides": [
          {
            "identifier": "total_usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "total_cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "10": {
      "title": "Capability / Cost center Heatmap",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = arraySum(cost)\n| fieldsAdd usage = arraySum(usage)\n//The summarize details would change based on intended tile\n| summarize {sum(usage), sum(cost)}, by:{event.type, filter}",
      "visualization": "heatmap",
      "visualizationSettings": {
        "dataMapping": {
          "xAxis": "filter",
          "yAxis": "event.type",
          "bucketValue": "sum(cost)"
        },
        "axes": {
          "yAxis": {
            "showLabel": true
          }
        },
        "legend": {
          "ratio": 5
        },
        "unitsOverrides": [
          {
            "identifier": "sum(cost)",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": false,
            "added": 1753286738031
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "11": {
      "type": "markdown",
      "content": "## Historical view of capabilities"
    },
    "12": {
      "title": "Estimated Costs per capability and $CostAllocation",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n//The summarize details would change based on intended tile\n//change resolution to the configured one -> \n| fieldsAdd rec = iCollectArray(record( start=start[], usage=usage[], event.type,filter, cost=cost[]))\n| fieldsKeep rec\n| expand rec\n| fieldsAdd usage = rec[usage], start = rec[start], event.type = rec[event.type], filter = rec[filter], cost = rec[cost]\n| makeTimeseries usage=sum(usage), cost=sum(cost),\n  interval:duration(parse($resolution,\"LONG:t\"),parse($resolution,\"LONG ALPHA:u\")), \n  time:bin(start,15m), \n  by:{event.type,filter}\n// <-",
      "visualization": "lineChart",
      "visualizationSettings": {
        "chartSettings": {
          "xAxisScaling": "analyzedTimeframe",
          "fieldMapping": {
            "leftAxisValues": [
              "cost"
            ]
          },
          "gapPolicy": "connect"
        },
        "dataMapping": {
          "displayedFields": [
            "filter",
            "event.type"
          ]
        },
        "autoSelectVisualization": false,
        "unitsOverrides": [
          {
            "identifier": "total_usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "total_cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 10000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 5,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "13": {
      "title": "Estimated Costs per capability",
      "description": "",
      "type": "data",
      "query": "//This is the \"base\" query for cost allocation overview dashboarding\n//We will look to move this and the dashboard to Monaco soon\n\nfetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filterOut event.type == \"Traces - Ingest & Process\" //traces ingest needs special handling\n| filterOut event.type == \"Metrics - Ingest & Process\" //metrics ingest needs special handling\n| filter in(event.type, {$Capability})\n| dedup event.id, event.type\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q, Data Egress\n    billed_invocations, //AppEngine Functions, AI Function Standard Call\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    `database-instance-hours`, // Database Monitoring\n    usage.quantity.billable,  // AI Units\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n| makeTimeseries interval: 15m, time: usage.start, nonempty:false, by:{filter, event.type }, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to),{\n  usage = sum(usage),\n  start = start()\n}\n// THIS IS JUST FOR TRACES INGEST NORMALIZATION ----->\n| append [\n   fetch dt.system.events, from: toTimestamp($dt_timeframe_from) - 1h, to: toTimestamp($dt_timeframe_to) + 1h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Traces - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | fieldsAdd filter = costallocation\n      | filter in(filter, {$Filter}) or isnull(filter)\n      | fieldsKeep usage.start, licensing_type, ingested_bytes, costallocation, event.id\n      | dedup event.id\n      | fieldsAdd licensing_type = if(in(licensing_type, {\n        \"otlp-trace-ingest\", \"serverless\"}), \"other\", else: licensing_type)\n\n      | fieldsAdd adaptive_volume = if(licensing_type == \"fullstack-adaptive\", ingested_bytes)\n      | fieldsAdd fixed_rate_volume = if(licensing_type == \"fullstack-fixed-rate\", ingested_bytes)\n      | fieldsAdd other_volume = if(licensing_type == \"other\", ingested_bytes)\n\n      | fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n\n      | makeTimeseries interval: 15m, time: usage.start, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), by: {\n        costallocation}, nonempty: true, {\n        adaptive_volume = sum(adaptive_volume, default: 0),\n        fixed_rate_volume = sum(fixed_rate_volume, default: 0),\n        other_volume = sum(other_volume, default: 0),\n        start = start()\n      }\n        // outer-join with FS-BUE-derived per-costallocation ratio of billed volume\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 10min\n          | filter event.kind == \"BILLING_USAGE_EVENT\"\n          | filter event.type == \"Full-Stack Monitoring\"\n          | fieldsAdd cap_type = \"Traces - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if trace ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fieldsRemove dt.cost.costcenter, dt.cost.product\n          | fieldsKeep usage.start, billed_gibibyte_hours, costallocation, event.id\n          | makeTimeseries {\n            billed = sum(billed_gibibyte_hours, default: 0) }, by: {\n            costallocation }, time: usage.start, interval: 15m, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | summarize by: {\n            timeframe, interval }, {\n            total = sum(billed[]), r = collectArray(record(costallocation, billed)) }\n          | expand r\n          | fieldsFlatten r, fields: {\n            costallocation, billed }\n          | fieldsAdd ratio = iCollectArray(coalesce((billed)[] / total[], 0.0))\n          | fields timeframe, interval, costallocation, ratio, billed, total\n          | fieldsAdd costallocation = if(isNull(costallocation), \"unassigned\", else: costallocation)\n      ], on: {\n         interval, costallocation }, kind: outer, prefix: \"included.\"\n       | fieldsAdd timeframe = coalesce(timeframe, included.timeframe)\n       | fieldsAdd interval = coalesce(interval, included.interval)\n       | fieldsAdd costallocation = coalesce(costallocation, included.costallocation)\n       // join environment-level included/configured volumes after the outer join\n       // so extra_ingest_on / included_volume apply uniformly to all rows\n       // (matched, trace-only and FS-only cost allocations)\n      | join [\n          timeseries interval: 15m, union: true, nonempty: true, bucket: {\"default_metrics\"}, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to), {\n            included_volume = max(dt.billing.traces.maximum_included_fullstack_volume_per_minute, default: 0),\n            configured_volume = max(dt.billing.traces.maximum_configured_fullstack_volume_per_minute, default: 0)\n          }\n          | fieldsAdd interval_in_minutes = toLong(interval) / 60000000000\n          | fieldsAdd extra_ingest_on = configured_volume[] > included_volume[]\n          | fieldsAdd included_volume = interval_in_minutes * if(isNull(configured_volume[]), null, else: included_volume[])\n      ], on: { interval }, fields: { included_volume, extra_ingest_on }\n      | join [\n      //this creates an array of 0 with the correct length based on timeframe\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n      ], on: { interval }, fields: { empty_included}, kind: outer\n      //this creates an array of false with the correct length based on timeframe\n      | join [\n        data\n          record(a = 0)\n          | makeTimeseries empty_included = avg(a, default: 0), interval: 15min, nonempty:true, time: now(), from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to)\n          | expand empty_included\n          | fieldsAdd all_false = empty_included==1\n          | summarize all_false = collectArray(all_false), by: interval\n      ], on: { interval }, fields: { all_false}\n      //In cases where there's a costallocation in our BUE's but not in the included, set extra_ingest_on to false for each interval\n      | fieldsAdd\n          extra_ingest_on =\n              if(exists(extra_ingest_on) and isNotNull(extra_ingest_on), extra_ingest_on,\n                else: all_false)\n      //We'll use this one to slice the others down to size\n      | fieldsAdd adaptive_volume = coalesce(iCollectArray(coalesce(adaptive_volume[], 0)), empty_included)\n      //Default the per-costallocation ratio to 0 for trace-only cost allocations (left-only outer-join rows)\n      | fieldsAdd ratio = coalesce(iCollectArray(coalesce(included.ratio[], 0)), empty_included)\n      //Compute included volume per cost allocation now that ratio applies uniformly to all rows\n      | fieldsAdd targetSize = if (arraySize(included_volume) > arraySize(ratio), then: arraySize(ratio), else: arraySize(adaptive_volume))\n      | fieldsAdd included_sliced = arraySlice(included_volume, to: targetSize)\n      | fieldsAdd ratio_sliced = arraySlice(ratio, to: targetSize)\n      | fieldsAdd volume_times_ratio = included_sliced[] * ratio_sliced[]\n      | fieldsAdd included_volume_costallocation = arraySlice(volume_times_ratio, to: arraySize(adaptive_volume))\n      //Replace nulls with 0\n      | fieldsAdd fixed_rate_volume = coalesce(iCollectArray(coalesce(fixed_rate_volume[], 0)), empty_included)\n      | fieldsAdd other_volume = coalesce(iCollectArray(coalesce(other_volume[], 0)), empty_included)\n      | fieldsKeep timeframe, interval, costallocation, adaptive_volume, fixed_rate_volume, other_volume,\n          included_volume_costallocation, extra_ingest_on, empty_included, start\n     // calculate fullstack trace-ingest usage per costcenter\n     | fieldsAdd included_volume_costallocation = arraySlice(included_volume_costallocation, to: arraySize(adaptive_volume)) //Since we're looking into the future with included, cut off any \"extra\"\n     | fieldsAdd license_remaining = included_volume_costallocation[] - adaptive_volume[]\n     | fieldsAdd license_remaining = if(license_remaining[] > 0, license_remaining[], else: 0)\n     | fieldsAdd extra_ingest_on = arraySlice(extra_ingest_on, to: arraySize(adaptive_volume)) //Depending on time frame, we can end up with an extra value here\n     | fieldsAdd\n          billable_fullstack =\n              if(isNull(included_volume_costallocation[]), 0,\n                else: if(extra_ingest_on[], adaptive_volume[] + fixed_rate_volume[] - included_volume_costallocation[],\n                  else: fixed_rate_volume[] - license_remaining[]))\n\n      | fieldsAdd billable_fullstack = if(billable_fullstack[] > 0, billable_fullstack[], else: 0)\n\n      | summarize {\n        total_included = sum(included_volume_costallocation[]),\n        total_adaptive = sum(adaptive_volume[]),\n        total_fixed_rate = sum(fixed_rate_volume[]),\n        total_fullstack_to_allocate = sum(billable_fullstack[]),\n        per_costallocation = collectArray(record(extra_ingest_on, billable_fullstack, billable_other = other_volume, costallocation, included_volume_costallocation, start))},\n        by: { interval }\n      | expand per_costallocation\n      | fieldsFlatten per_costallocation\n      | fieldsRemove per_costallocation\n\n      | fieldsAdd total_license_remaining = total_included[] - total_adaptive[]\n      | fieldsAdd total_license_remaining = if(total_license_remaining[] > 0, total_license_remaining[], else: 0)\n      | fieldsAdd\n          total_applicable_fullstack =\n              if(isNull(per_costallocation.included_volume_costallocation), 0,\n                else: if(per_costallocation.extra_ingest_on[], total_adaptive[] + total_fixed_rate[] - total_included[],\n                  else: total_fixed_rate[] - total_license_remaining[]))\n      | fieldsAdd total_billable_fullstack = if(total_applicable_fullstack[] > 0, total_applicable_fullstack[], else: 0)\n\n      | fieldsAdd\n          distributed_fullstack =\n              if(total_fullstack_to_allocate[] <= 0, 0,\n                else: per_costallocation.billable_fullstack[] / total_fullstack_to_allocate[] * total_billable_fullstack[])\n      // filter out  entry if the included was not fetched\n      | fieldsAdd\n          adjusted_billable_total_per_costallocation =\n              per_costallocation.billable_other[] + toDouble(distributed_fullstack[])\n     | filterOut isNull(adjusted_billable_total_per_costallocation) or isNull(per_costallocation.start) or IsNull(per_costallocation.costallocation)\n     | fields interval, costallocation = per_costallocation.costallocation, adjusted_billable_total_per_costallocation, start=per_costallocation.start\n     | fieldsAdd event.type =\"Traces - Ingest & Process\"\n     | fieldsAdd filter = costallocation\n]\n| fieldsAdd traces_usage = adjusted_billable_total_per_costallocation[] / (1024*1024*1024)\n// <---------THIS IS JUST FOR TRACES INGEST NORMALIZATION\n\n// THIS IS JUST FOR METRICS INGEST NORMALIZATION ----->\n| append [\n      fetch dt.system.events, from: toTimestamp($dt_timeframe_from) + 4h, to: toTimestamp($dt_timeframe_to) + 4h\n      | filter event.kind == \"BILLING_USAGE_EVENT\" and event.type == \"Metrics - Ingest & Process\"\n      | filter in(event.type, {$Capability})\n      | dedup event.id, event.type\n      | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n      | fieldsRemove dt.cost.costcenter, dt.cost.product\n      | expand costallocation\n      | fieldsFlatten costallocation\n      | fields {\n        usage.start,\n        costallocation = if(isNull(costallocation.key), $unassigned, else: costallocation.key),\n        data_points = toLong(costallocation.data_points),\n        monitoring_source = if(monitoring_source == \"fullstack\" or monitoring_source == \"infrastructure\", monitoring_source, else: \"other\")\n      }\n      | summarize {\n        data_points = toLong(sum(data_points))\n      }, by: {\n        usage.start,\n        costallocation,\n        monitoring_source\n      }\n      | join [\n          fetch dt.system.events, from: toTimestamp($dt_timeframe_from), to: toTimestamp($dt_timeframe_to) + 1h\n          | filter event.kind == \"BILLING_USAGE_EVENT\" and in(event.type, { \"Full-Stack Monitoring\", \"Infrastructure Monitoring\" })\n          | fieldsAdd cap_type = \"Metrics - Ingest & Process\"\n          | filter in(cap_type, {$Capability}) //only query if metrics ingest selected\n          | dedup event.id, event.type\n          | fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n          | fields {\n            usage.start,\n            costallocation = if(isNotNull(costallocation), costallocation, else: $unassigned),\n            billed_value = coalesce(if(exists(billed_gibibyte_hours), billed_gibibyte_hours, else: null), if(exists(billed_host_hours), billed_host_hours, else: null)),\n            monitoring_source = if(event.type == \"Full-Stack Monitoring\", \"fullstack\", else: if(event.type == \"Infrastructure Monitoring\", \"infrastructure\"))\n          }\n          | summarize {\n            billed = sum(billed_value)\n          }, by: {\n            usage.start,\n            costallocation,\n            monitoring_source\n          }\n          | fields {\n            usage.start,\n            costallocation,\n            monitoring_source,\n            billed,\n            included_usage =  billed * 4 * if(monitoring_source == \"fullstack\", 900, else: if(monitoring_source == \"infrastructure\", 1500, else: 0))\n          }\n      ], on: { usage.start, costallocation, monitoring_source }, kind:leftOuter, fields: { included_usage }\n      | fieldsAdd included_usage = if(isNotNull(included_usage), included_usage, else: 0)\n      | fieldsAdd billable = if(data_points > included_usage, data_points - included_usage, else: 0)\n      | summarize {\n        data_points_total = sum(data_points),\n        included_total = sum(included_usage),\n        billable_per_costallocation_total = sum(billable),\n        rec = collectArray(record(costallocation, billable, included_usage))\n      }, by: {\n        usage.start,\n        monitoring_source\n      }\n      | fieldsAdd actual_billable_total = if(data_points_total > included_total, data_points_total - included_total, else: 0)\n      | expand rec\n      | fieldsAdd ratio_for_cost_allocation = rec[billable] / billable_per_costallocation_total\n      | fieldsAdd charged_per_costcenter = ratio_for_cost_allocation * actual_billable_total\n      | fields usage = charged_per_costcenter, usage.start, costallocation = rec[costallocation]\n      | makeTimeseries {usage = sum(usage), start=start()}, from:toTimestamp($dt_timeframe_from), to:toTimestamp($dt_timeframe_to), time:usage.start, interval: 15m, by:{costallocation}\n      | fieldsAdd filter = costallocation\n      | fieldsAdd event.type = \"Metrics - Ingest & Process\"\n      | filter in(filter, {$Filter})\n]\n// <---------THIS IS JUST FOR METRICS INGEST NORMALIZATION\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd isnull = isNull(adjusted_billable_total_per_costallocation)\n| fieldsAdd usage = if(isnull, usage, else: traces_usage)\n| fieldsAdd cost = usage[] * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n\n//change resolution to the configured one -> \n| fieldsAdd rec = iCollectArray(record( start=start[], usage=usage[], event.type, cost=cost[]))\n| fieldsKeep rec\n| expand rec\n| fieldsAdd usage = rec[usage], start = rec[start], event.type = rec[event.type], cost = rec[cost]\n| makeTimeseries usage=sum(usage), cost=sum(cost),\n  interval:duration(parse($resolution,\"LONG:t\"),parse($resolution,\"LONG ALPHA:u\")), \n  time:bin(start,15m), \n  by:{event.type}\n// <-\n\n | summarize {\n        total_usage = sum(usage[]),\n        total_cost = sum(cost[])},\n        by: { timeframe, interval, event.type }\n| sort event.type",
      "visualization": "lineChart",
      "visualizationSettings": {
        "chartSettings": {
          "xAxisScaling": "analyzedTimeframe",
          "fieldMapping": {
            "leftAxisValues": [
              "total_cost"
            ]
          },
          "gapPolicy": "connect"
        },
        "dataMapping": {
          "displayedFields": [
            "event.type"
          ]
        },
        "autoSelectVisualization": false,
        "unitsOverrides": [
          {
            "identifier": "usage",
            "unitCategory": "amount",
            "baseUnit": "one",
            "displayUnit": null,
            "decimals": 0,
            "suffix": "",
            "delimiter": false,
            "added": 1754648177039
          },
          {
            "identifier": "cost",
            "unitCategory": "currency",
            "baseUnit": "usd",
            "displayUnit": null,
            "decimals": 2,
            "suffix": "",
            "delimiter": true,
            "added": 1754648199722
          }
        ]
      },
      "querySettings": {
        "maxResultRecords": 5000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "14": {
      "type": "markdown",
      "content": "## Maximum detail level for usage"
    },
    "15": {
      "title": "",
      "type": "data",
      "query": "fetch dt.system.events\n| filter event.kind == \"BILLING_USAGE_EVENT\"\n| filter in(event.type, {$Capability})\n| lookup [\n   load \"/lookups/CA/userdata\"\n], sourceField:user.email, lookupField:user.email\n| lookup [ //this is for workflows... \n   load \"/lookups/CA/userdata\"\n], prefix:\"wfca.\", sourceField:workflow.actor, lookupField:user.id\n| fieldsAdd dt.cost.costcenter = coalesce(dt.cost.costcenter, lookup.dt.cost.costcenter, wfca.dt.cost.costcenter, $unassigned)\n| fieldsAdd dt.cost.product = coalesce(dt.cost.product, lookup.dt.cost.product,wfca.dt.cost.product, $unassigned)\n| fieldsAdd costallocation = if($CostAllocation==\"costcenter\",dt.cost.costcenter, else: dt.cost.product)\n| fieldsAdd costallocation = if(isNull(costallocation), $unassigned, else: costallocation)\n| fieldsRemove dt.cost.costcenter, dt.cost.product\n| expand costallocation\n| fieldsAdd filter = coalesce(costallocation[key], costallocation)\n| filter in(filter, {$Filter}) or isNull(filter)\n| fieldsAdd billed_bytes = coalesce(costallocation[billed_bytes], billed_bytes)\n| fieldsAdd usage = coalesce(\n    billed_host_hours, //Infra, F&D\n    billed_gibibyte_hours, //Fullstack, Runtime Application Protection, Runtime Vulnerability Analytics\n    toDouble(billed_bytes) / (1024*1024*1024), //ALL I/R/Q\n    billed_invocations, //AppEngine\n    ingested_bytes, //Traces I\n    billed_sessions, //RUM sessions\n    billed_replay_sessions, //rum replay sessions\n    billed_property_sessions, //rum properties\n    billed_pod_hours, //k8s platform monitoring\n    billed_container_hours, //Code Monitoring\n    billed_synthetic_action_count, //Browser Monitor\n    billed_http_request_count, //HTTP Monitor\n    billed_msu_hours, //Mainframe\n    1 //workflow count\n    )\n| fieldsAdd usage = if(contains(event.type,\"Retain\"),usage / 24, else: usage)\n| fieldsAdd entityName(dt.entity.host)\n| fieldsAdd usage.start = if(isNull(usage.start), timestamp, else: usage.start)\n\n| lookup [\n   data json:$price_points\n], sourceField:event.type, lookupField:name, prefix: \"ratecard.\"\n| fieldsAdd cost = usage * toDouble(ratecard.price) / toDouble(ratecard.priceUnit)\n| fieldsAdd cost = if(event.type == \"Traces - Ingest & Process\" or event.type\n ==\"Metrics - Ingest & Process\", \"not applicable*\", else:cost)\n| fieldsRename currency = ratecard.currencyCode, capability = event.type, price = ratecard.price, category = ratecard.Category, priceUnit = ratecard.priceUnit\n| fieldsKeep usage.start, category, capability, filter, usage, cost, price, \n      priceUnit, currency, dt.entity.host.name, application_only_type, \n      dt.entity.host, k8s.cluster.uid, \n      k8s.namespace.name, ingested_bytes, ingested_spans, licensing_type, user.email, \n      client.application_context, client.function_context, dt.entity.device_application,\n      dt.entity.application\n| sort filter, usage.start, capability",
      "visualization": "table",
      "visualizationSettings": {
        "table": {
          "hiddenColumns": [
            [
              "event.version"
            ],
            [
              "dt.openpipeline.source"
            ]
          ],
          "hideColumnsForLargeResults": false,
          "sortBy": [
            {
              "columnId": "[\"capability\"]",
              "direction": "ascending"
            }
          ],
          "columnWidths": {
            "[\"capability\"]": 232.19,
            "[\"dt.entity.host.name\"]": 219
          },
          "columnOrder": [
            "[\"usage.start\"]",
            "[\"category\"]",
            "[\"capability\"]",
            "[\"filter\"]",
            "[\"usage\"]",
            "[\"cost\"]",
            "[\"currency\"]",
            "[\"dt.entity.host.name\"]",
            "[\"price\"]",
            "[\"priceUnit\"]",
            "[\"k8s.namespace.name\"]",
            "[\"application_only_type\"]",
            "[\"dt.entity.host\"]",
            "[\"k8s.cluster.uid\"]"
          ]
        }
      },
      "querySettings": {
        "maxResultRecords": 1000,
        "defaultScanLimitGbytes": 500,
        "maxResultMegaBytes": 1,
        "defaultSamplingRatio": 10,
        "enableSampling": false
      },
      "davis": {
        "enabled": false,
        "davisVisualization": {
          "isAvailable": true
        }
      }
    },
    "16": {
      "type": "markdown",
      "content": "*shown trace usage also includes included, non billable usage. The calculation for billable Traces cannot be done on record level. Therefore costs cannot be calculated within this table. For the Trace Ingest usage and costs which is billable - check the other tiles please."
    }
  },
  "layouts": {
    "0": {
      "x": 0,
      "y": 0,
      "w": 12,
      "h": 5
    },
    "1": {
      "x": 12,
      "y": 0,
      "w": 12,
      "h": 3
    },
    "2": {
      "x": 0,
      "y": 5,
      "w": 12,
      "h": 2
    },
    "3": {
      "x": 12,
      "y": 5,
      "w": 6,
      "h": 7
    },
    "4": {
      "x": 0,
      "y": 12,
      "w": 12,
      "h": 7
    },
    "5": {
      "x": 12,
      "y": 12,
      "w": 6,
      "h": 7
    },
    "6": {
      "x": 0,
      "y": 19,
      "w": 24,
      "h": 6
    },
    "7": {
      "x": 0,
      "y": 25,
      "w": 24,
      "h": 2
    },
    "8": {
      "x": 0,
      "y": 27,
      "w": 12,
      "h": 7
    },
    "9": {
      "x": 12,
      "y": 27,
      "w": 12,
      "h": 11
    },
    "10": {
      "x": 0,
      "y": 38,
      "w": 12,
      "h": 4
    },
    "11": {
      "x": 0,
      "y": 42,
      "w": 24,
      "h": 2
    },
    "12": {
      "x": 0,
      "y": 44,
      "w": 12,
      "h": 5
    },
    "13": {
      "x": 12,
      "y": 44,
      "w": 12,
      "h": 5
    },
    "14": {
      "x": 0,
      "y": 49,
      "w": 24,
      "h": 1
    },
    "15": {
      "x": 0,
      "y": 50,
      "w": 24,
      "h": 10
    },
    "16": {
      "x": 0,
      "y": 60,
      "w": 24,
      "h": 1
    }
  },
  "importedWithCode": false,
  "settings": {},
  "annotations": []
}
